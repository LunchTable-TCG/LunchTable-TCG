# Frontend Convex target (copy to apps/web/.env.local or run `bun run setup:env`).
VITE_CONVEX_URL=https://scintillating-mongoose-458.convex.cloud

# Optional dev-only API-key auth bridge.
# Only honored on localhost/127.0.0.1 with ?devAgent=1 in Vite dev mode.
# VITE_DEV_AGENT_API_KEY=ltcg_your_local_dev_agent_key
